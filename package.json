{
  "name": "push-notifier",
  "version": "1.0.0",
  "description": "Notifier of pushes",
  "author": "<stevewinton@gmail.com> (https://github.com/swinton)",
  "license": "ISC",
  "repository": "https://github.com/swinton/push-notifier.git",
  "homepage": "https://github.com/swinton/push-notifier",
  "bugs": "https://github.com/swinton/push-notifier/issues",
  "keywords": [
    "probot",
    "github",
    "probot-app"
  ],
  "scripts": {
    "dev": "nodemon --exec \"npm start\"",
    "start": "probot run ./index.js",
    "lint": "standard --fix",
    "test": "jest && standard",
    "test:watch": "jest --watch --notify --notifyMode=change --coverage"
  },
  "dependencies": {
    "probot": "^6.0.0"
  },
  "devDependencies": {
    "jest": "^22.4.3",
    "nodemon": "^1.17.2",
    "smee-client": "^1.0.2",
    "standard": "^10.0.3"
  },
  "engines": {
    "node": ">= 8.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
